[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pandas",
    "numpy",
    "matplotlib",
    "seaborn",
    "scipy",
    "adjustText",
    "psutil" 
]
build-backend = "setuptools.build_meta"
